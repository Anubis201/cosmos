import { EditElementDirective } from './edit-element.directive';

describe('EditElementDirective', () => {
  it('should create an instance', () => {
    const directive = new EditElementDirective();
    expect(directive).toBeTruthy();
  });
});
